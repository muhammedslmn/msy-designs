node_modules/
# Everything under build/ is regenerated by the pipeline: frame sequences,
# intermediate clips, the TTS scratch track and the final MP4s.
build/
src/fonts.css
# x264 two-pass scratch, written next to the cwd rather than into build/
ffmpeg2pass-*.log
ffmpeg2pass-*.log.mbtree
site/*.html
